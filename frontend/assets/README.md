# SURKH assets

Drop every file in this zip straight into your project's `assets/` folder
(same flat structure — no subfolders) and every path in `index.html` /
`style.css` will resolve automatically. Nothing to rename, nothing to edit.

Placeholder icons (built by me, not sourced from your uploads) — swap these
out any time by replacing the file, same filename:
- icon-car.svg
- icon-pin.svg
- icon-search.svg

Everything else is a real asset you uploaded during the build.
